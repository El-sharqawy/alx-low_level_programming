#include "main.h"

/**
 * _abs - Check Main
 * @n: An input integer
 * Description: function computes the absolute value of an integer.
 * Return: absolute value.
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
